<Window x:Class="GprPrediction.Wpf.Windows.PrintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="출력 및 보고서"
        Height="820"
        Width="1400"
        MinHeight="680"
        MinWidth="960"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        WindowStartupLocation="CenterOwner">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="46" ResizeBorderThickness="5"
                      GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.CommandBindings>
        <CommandBinding Command="{x:Static SystemCommands.MinimizeWindowCommand}" Executed="MinimizeWindow" />
        <CommandBinding Command="{x:Static SystemCommands.MaximizeWindowCommand}" Executed="MaximizeRestoreWindow" />
        <CommandBinding Command="{x:Static SystemCommands.RestoreWindowCommand}" Executed="MaximizeRestoreWindow" />
        <CommandBinding Command="{x:Static SystemCommands.CloseWindowCommand}" Executed="CloseWindow" />
    </Window.CommandBindings>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{StaticResource TitleBarBackgroundBrush}"
                BorderThickness="0">
            <Grid Height="46">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0,0,0">
                    <Image Source="pack://application:,,,/Assets/GprRadar.png"
                           Width="24"
                           Height="24"
                           Margin="0,-1,10,0"
                           VerticalAlignment="Center" />
                    <TextBlock Text="GPR"
                               FontSize="17"
                               FontWeight="Black"
                               Foreground="{StaticResource AccentBrush}"
                               Margin="0,-1,0,0"
                               VerticalAlignment="Center" />
                    <Rectangle Width="1"
                               Height="18"
                               Fill="{StaticResource BorderBrush1}"
                               Margin="12,0"
                               VerticalAlignment="Center"
                               Opacity="0.5" />
                    <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch">
                    <Button Command="{x:Static SystemCommands.MinimizeWindowCommand}"
                            Style="{StaticResource TitleBarButton}"
                            Content="&#xE921;" />
                    <Button Command="{x:Static SystemCommands.MaximizeWindowCommand}"
                            Style="{StaticResource TitleBarButton}">
                        <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="10">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="&#xE922;" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}"
                                                     Value="Maximized">
                                            <Setter Property="Text" Value="&#xE923;" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Button>
                    <Button Command="{x:Static SystemCommands.CloseWindowCommand}"
                            Style="{StaticResource TitleBarCloseButton}"
                            Content="&#xE8BB;" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="16" Background="{StaticResource BackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource Card}" Padding="18,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="4"
                                    Height="16"
                                    Background="{StaticResource AccentBrush}"
                                    CornerRadius="2"
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center" />
                            <TextBlock Text="출력 및 보고서"
                                       FontSize="22"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                        </StackPanel>
                        <TextBlock Margin="14,10,0,0"
                                   Text="현재 분석 이미지, 결과값, 로그를 묶어서 인쇄하거나 보고서 폴더로 저장합니다."
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="12" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="보고서 저장"
                                Width="110"
                                Height="34"
                                Margin="0,0,8,0"
                                Style="{StaticResource SecondaryButton}"
                                Click="SaveReport_Click" />
                        <Button Content="인쇄"
                                Width="96"
                                Height="34"
                                Style="{StaticResource PrimaryButton}"
                                Click="Print_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.35*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="0.95*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                BorderBrush="{StaticResource BorderBrush1}"
                                BorderThickness="0,0,0,1"
                                Padding="16,14">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="3"
                                        Height="14"
                                        Background="#00D4AA"
                                        CornerRadius="2"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center" />
                                <TextBlock Text="보고서 미리보기"
                                           FontSize="12"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                            </StackPanel>
                        </Border>

                        <ScrollViewer Grid.Row="1"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <Border x:Name="ReportPreviewRoot"
                                    Margin="16"
                                    Width="760"
                                    Padding="20"
                                    Background="White"
                                    BorderBrush="#C9CDD4"
                                    BorderThickness="1"
                                    CornerRadius="2">
                                <StackPanel>
                                    <TextBlock Text="결과 출력 양식"
                                               FontSize="28"
                                               FontWeight="Normal"
                                               Foreground="#111111" />

                                    <Grid Margin="0,18,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="GPR Data Labeling"
                                                   Margin="0,4,12,0"
                                                   FontSize="11"
                                                   Foreground="#333333"
                                                   VerticalAlignment="Top" />

                                        <Border Grid.Column="1"
                                                Padding="0"
                                                Background="#0E121A"
                                                BorderBrush="#D6D9E0"
                                                BorderThickness="1">
                                            <Image Source="{Binding AnalysisImage}"
                                                   Height="185"
                                                   Stretch="Uniform" />
                                        </Border>
                                    </Grid>

                                    <Grid Margin="0,10,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Top View"
                                                   Margin="0,4,12,0"
                                                   FontSize="11"
                                                   Foreground="#333333"
                                                   VerticalAlignment="Top" />

                                        <Border Grid.Column="1"
                                                Background="#0F1320"
                                                BorderBrush="#D6D9E0"
                                                BorderThickness="1"
                                                Padding="0">
                                            <Canvas x:Name="ReportTopViewCanvas"
                                                    Height="200"
                                                    Background="#0F1320"
                                                    SizeChanged="ReportCanvas_SizeChanged" />
                                        </Border>
                                    </Grid>

                                    <Grid Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Front View"
                                                   Margin="0,4,12,0"
                                                   FontSize="11"
                                                   Foreground="#333333"
                                                   VerticalAlignment="Top" />

                                        <Border Grid.Column="1"
                                                Background="#0F1320"
                                                BorderBrush="#D6D9E0"
                                                BorderThickness="1"
                                                Padding="0">
                                            <Canvas x:Name="ReportFrontViewCanvas"
                                                    Height="200"
                                                    Background="#0F1320"
                                                    SizeChanged="ReportCanvas_SizeChanged" />
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource Card}" Padding="16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="3"
                                        Height="14"
                                        Background="#7C4DFF"
                                        CornerRadius="2"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center" />
                                <TextBlock Text="요약"
                                           FontSize="12"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                            </StackPanel>

                            <TextBlock Text="선택 지도"
                                       Margin="0,14,0,0"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="11" />
                            <TextBlock Text="{Binding MapDwgPath}"
                                       Margin="0,4,0,0"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource TextPrimaryBrush}" />

                            <TextBlock Text="불러온 저장 결과"
                                       Margin="0,14,0,0"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="11" />
                            <TextBlock Text="{Binding LoadedSavedResultText}"
                                       Margin="0,4,0,0"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                                    BorderBrush="{StaticResource BorderBrush1}"
                                    BorderThickness="0,0,0,1"
                                    Padding="16,14">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="3"
                                            Height="14"
                                            Background="#FFB300"
                                            CornerRadius="2"
                                            Margin="0,0,8,0"
                                            VerticalAlignment="Center" />
                                    <TextBlock Text="콘솔 로그"
                                               FontSize="12"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Row="1"
                                    Margin="16"
                                    Background="#0C1430"
                                    BorderBrush="{StaticResource BorderBrush1}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="1">
                                <TextBox Text="{Binding LogText}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{StaticResource TextPrimaryBrush}"
                                         CaretBrush="{StaticResource TextPrimaryBrush}"
                                         Margin="14"
                                         IsReadOnly="True"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         VerticalAlignment="Stretch"
                                         VerticalScrollBarVisibility="Auto"
                                         HorizontalScrollBarVisibility="Disabled"
                                         FontFamily="Consolas"
                                         FontSize="12" />
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

        </Grid>
    </Grid>
</Window>
