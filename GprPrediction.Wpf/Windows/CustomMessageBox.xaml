<Window x:Class="GprPrediction.Wpf.Windows.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height"
        Width="540"
        MaxWidth="760"
        MaxHeight="640"
        WindowStartupLocation="CenterOwner">
    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush1}"
            BorderThickness="1"
            CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect Color="#000000"
                              BlurRadius="32"
                              ShadowDepth="10"
                              Opacity="0.48"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{DynamicResource TitleBarBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush1}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="14,14,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="16,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="38"/>
                    </Grid.ColumnDefinitions>
                    <Image Width="20" Height="20"
                           Source="/GprPrediction.Wpf;component/Assets/gpr_icon.png"
                           Stretch="Uniform"/>
                    <TextBlock x:Name="CaptionText"
                               Grid.Column="1"
                               Margin="10,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="2"
                            Width="28"
                            Height="28"
                            Content="&#x2715;"
                            ToolTip="닫기"
                            Click="Close_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            FontSize="12"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="24,24,24,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="46"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="IconBackground"
                        Width="36"
                        Height="36"
                        VerticalAlignment="Top"
                        CornerRadius="18">
                    <TextBlock x:Name="IconText"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="White"
                               FontSize="18"
                               FontWeight="Bold"/>
                </Border>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox x:Name="MessageText"
                             MaxHeight="420"
                             IsReadOnly="True"
                             IsReadOnlyCaretVisible="True"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="0,2,8,2"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             FontSize="13"
                             SelectionBrush="{DynamicResource AccentBrush}"/>

                    <Border Grid.Row="1"
                            Margin="0,18,0,14"
                            Height="1"
                            Background="{DynamicResource BorderBrush1}"/>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="내용 복사"
                                Padding="14,7"
                                Style="{DynamicResource SecondaryButton}"
                                Click="Copy_Click"/>
                        <StackPanel x:Name="ButtonPanel"
                                    Grid.Column="2"
                                    Orientation="Horizontal"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
